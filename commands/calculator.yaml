name: "Calculator"
desc: "Perform some simple calculation"
activators:
  - "calc"
steps:
  - action: Plus
    args:
      - "!1"
      - "!2"
    pushResult: true
  - action: Reply
    args:
      - "!1 + !2 = !pop"
