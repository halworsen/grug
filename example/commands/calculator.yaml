name: "Calculator"
desc: "Perform some simple calculation"
activators:
  - "calc"
steps:
  - action: Plus
    args:
      - "!1"
      - "!2"
    push: true
  - action: Reply
    args:
      - "!1 + !2 = !pop"
